/**
 * Sum of two numbers
 *
 * @param {number} m first number
 * @param {number} n second number
 * @returns {number}
 */
export const sum = (m, n) => {
  let number1 = +m;
  let number2 = +n;
  return number1 + number2;
};
